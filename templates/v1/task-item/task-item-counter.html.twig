<div class="col-md-12 ti ti-counter{{  task_item.completed ? 'completed' }}" id="item-id-{{ task_item.id }}">
    <div class="row">
        <div class="col-md-6">
            <div class="tin">{{ task_item.name }}</div>
        </div>
        <div class="tid col-md-3">
            <div class="tiq">{{ task_item.qty }}</div>
            {{ form(increment_item_forms[task_item.id]) }}
        </div>
    </div>
</div>