{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <p>Total users: {{ users|length }}</p>
    {% for user in users %}
        <p>{{ user.email }} ({{ user.taskLists|length }} lists) {{ user.lastLogin|date }}</p>git
    {% endfor %}
{% endblock %}